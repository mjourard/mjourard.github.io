os: linux
dist: xenial
language: node_js
node_js:
    - 14
script:
    - npm run test
deploy:
    strategy: git
    provider: pages
    skip_cleanup: true
    github_token: $PERSONAL_GITHUB_ACCESS_TOKEN
    keep_history: true
    on:
        branch: master
    target_branch: gh-pages
